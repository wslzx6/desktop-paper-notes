# 测试说明

项目通过 `build.ps1 -Test` 构建并运行自包含回归检查。当前版本共 66 项检查。

## 覆盖范围

- Unicode、emoji、负坐标和样式字段的 JSON 往返保存
- 最近历史版本、上一版备份、主文件损坏恢复和无法恢复时的只读保护
- Markdown 标题、粗体、斜体、删除线、任务、引用、链接、代码块和纯文本摘要
- Markdown 工具栏对选区的添加、取消及多行操作
- 每张便签独立透明度以及原生桌面窗口 alpha 更新
- 多显示器坐标、断开显示器后的可见区域恢复
- Explorer 桌面宿主连接、图标/便签/壁纸层级和点击穿透窗口样式
- 后台锁定启动、多便签增删、撤销删除和重新打开管理窗口
- 关闭窗口后后台继续运行以及完整退出后的数据恢复
- 管理窗口与桌面原位编辑器的 Markdown 工具栏
- 高 DPI 下标签、输入区域、透明度控件、缩放柄和操作按钮布局
- 普通按钮保持系统箭头光标

## 已验证环境

- 64 位 Windows 11
- 2560 × 1600 单显示器
- 150% DPI 缩放
- Windows Explorer 桌面
- Wallpaper Engine 动态壁纸

桌面宿主检查依赖真实 Windows Shell，不适合在无桌面的持续集成容器中运行。数据和 Markdown 的纯逻辑检查可以拆分后用于 CI。

## 运行

先从托盘退出正在运行的正式版，然后执行：

```powershell
.\build.ps1 -Test
```

结果写入构建目录的 `test-results/latest.txt`。测试使用隔离数据目录，不应接触正式便签数据。
