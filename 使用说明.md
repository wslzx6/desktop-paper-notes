# 桌面便签

双击 `dist\桌面便签.exe` 即可使用，无需安装开发工具或管理员权限。适用于 64 位 Windows 10 / 11，需要系统自带的 .NET Framework 4.8。

## 日常使用

1. 点击「新建」，创建多个便签。在管理窗口修改标题、内容、颜色、字号和透明度，修改会自动保存。内容支持 Markdown，编辑时输入源码，桌面便签自动显示排版效果。也可以先选择一段文字，再点击编辑框上方的粗体、斜体、删除线等按钮，软件会自动添加或移除对应语法。
2. 点击「去桌面摆放」：主窗口最小化，桌面仍处于编辑状态。拖动便签标题栏移动；拖动右下角调整大小；双击正文直接在原位置输入。桌面图标优先响应操作，覆盖在图标下的便签可通过管理窗口选中并编辑。
3. 输入时按 `Esc`、`Ctrl+Enter`、右上角的勾号或点击其他窗口，结束当前便签编辑。中文输入法、换行和复制粘贴使用系统输入框。
4. 关闭主窗口或点击「完成并锁定」：软件留在托盘，便签回到壁纸上方、图标下方，完全不接收鼠标键盘操作。
5. 双击托盘图标或右键选择「打开编辑」，恢复编辑。再次双击软件也会打开已有窗口，不启动重复实例。
6. 托盘「退出软件」结束后台进程，便签随之消失，数据保留。

## 开机与保存

首次正常运行默认启用当前用户的开机启动；启动时只恢复锁定便签，不弹出窗口。可在主窗口取消「开机启动」。开机启动命令指向当前 exe 路径，请保持文件位置；移动后正常打开一次会更新路径。

数据目录固定为 exe 旁的 `data` 文件夹（本项目为 `dist\data`），使用绝对路径，不随启动方式或工作目录改变。首次使用这个版本时会迁移 `%LOCALAPPDATA%\DesktopPaperNotes` 中的旧数据，旧文件保留；后续启动只使用新目录。移动软件时请同时移动 `data` 文件夹，升级时保留该文件夹。

`versions` 中保留最近 30 次完整保存，采用独立文件写入并刷新磁盘，不依赖数据目录是否支持替换或重命名。启动时优先读取最后一份可用保存；最新文件不完整时恢复前一份。

`notes.json` 和 `notes.json.bak` 是便于查看的当前及上一版副本。历史保存不存在时仍兼容读取这两个文件。损坏文件会保留；无法恢复时暂停保存，避免覆盖原数据。迁移时可导出备份，或复制整个数据目录。

「导出备份」保存 JSON 文件；「导入备份」替换当前便签前，先保留一份当前数据。删除便签后可以用「撤销删除」恢复；撤销记录仅在本次运行中保留。

支持多个显示器，包括位于主屏左侧的显示器。断开显示器、改变分辨率后，超出显示范围的便签会移回可见区域。Explorer 重启后会自动尝试重新连接桌面。不会更改原壁纸。

## 显示行为

便签内容支持常用 Markdown：`#` / `##` / `###` 标题、`**粗体**`、`*斜体*`、`~~删除线~~`、项目符号和数字列表、`- [ ]` / `- [x]` 任务框、`>` 引用、行内代码、围栏代码块、分隔线以及 `[文字](网址)` 链接。锁定的便签不接收点击，因此链接只显示样式，不会打开网页；编辑框中始终保留原始 Markdown 文本。

管理窗口和桌面原位编辑窗口都有 Markdown 工具栏。没有选择文字时，粗体、斜体、删除线和代码按钮会插入成对标记并把光标留在中间；再次选中包含标记的文字并点击同一按钮，可以移除该格式。标题、引用和列表按钮按当前行或选中的多行操作。

选中便签后，拖动详情中的「透明度」滑块即可实时调整。0% 为完全不透明，100% 为完全透明；背景和文字一起变化。每张便签单独保存设置，关闭窗口和重新启动后保持不变。完全透明的便签仍可在管理窗口选中并调回可见。直接输入内容时，编辑窗口暂时保持不透明，结束输入后恢复设置。

便签的常驻显示层位于桌面图标下方。编辑某张便签时，该便签暂时使用前台输入窗口，结束输入立即回到桌面层。普通应用窗口会覆盖桌面便签。

Windows 的桌面宿主属于未公开的 Shell 实现，不同系统更新可能影响桌面连接；连接失败时状态栏提示并自动重试，便签数据继续保留。Windows 11 下使用图标窗口与动态壁纸窗口之间的独立便签层，并通过常规窗口绘制保持后台可见。退出软件、锁屏和系统启动期间，便签可能暂时不可见。

## 从源码构建

在项目文件夹运行：

```powershell
.\build.ps1
.\build.ps1 -Test
```

源码位于 `src`，构建结果位于 `dist`。无 NuGet 或网络依赖。构建前从托盘退出软件。

`--background`：后台启动并锁定；`--test-mode`：使用 exe 旁独立的测试数据，不修改开机启动；`--self-test`：运行数据和真实桌面宿主验证，结果保存到 exe 旁 `test-results`；`--diagnose`：生成桌面窗口层级诊断。
